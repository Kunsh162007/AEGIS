# ── Core (required to run the offline MVP) ───────────────────────────────
pydantic>=2.6
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.29
sse-starlette>=2.1
networkx>=3.2

# ── Public-dataset loading for the accuracy benchmark (Section 9) ─────────
pandas>=2.2

# ── Production model providers (OpenAI-compatible: AI/ML API, Featherless) ─
openai>=1.30            # used as a generic OpenAI-compatible client

# ── Optional: a stronger ML baseline for the eval (Section 9). Skip on
# Python 3.14 if wheels are unavailable — the heuristic baseline works without it.
# scikit-learn>=1.4
# lightgbm>=4.3

# ── Agent frameworks (the "two frameworks on purpose" scoring story) ─────
# The core pipeline runs WITHOUT these (USE_FRAMEWORKS=false). Install to enable
# the production path: the verification trio then runs as a REAL LangGraph
# StateGraph and the specialists as a CrewAI crew (orchestrator branches on the
# flag; each adapter degrades gracefully if its lib is missing).
#   pip install langgraph        # verification graph — runs offline, no keys
#   pip install crewai           # specialist crew (heavier; may need a model key)
# langgraph>=0.2
# crewai>=0.30

# ── Knowledge base (optional; falls back to built-in keyword store) ──────
# chromadb>=0.5
# sentence-transformers>=2.7

# ── Band SDK (added from the official Band SDK docs — package name TBD) ─
# band-sdk>=0.0            # <-- replace with the real package once docs are in hand
